/**
 * ============================================================================
 * SHIV COMPUTER - FIREBASE CONFIGURATION ADAPTER
 * ============================================================================
 *
 * Delegates directly to the root /src/firebase.ts utility module.
 */

export * from '../firebase';
import firebaseUtil from '../firebase';
export default firebaseUtil;
